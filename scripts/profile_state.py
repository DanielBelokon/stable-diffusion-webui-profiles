import modules
import os


profiles_settings_path = os.path.join(modules.scripts.basedir(), "profiles_settings.json")
profiles_settings_data = {"profile": "config.json", "profile_list": ["config.json"]}
